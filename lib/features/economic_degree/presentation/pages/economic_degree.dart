import 'package:flutter/material.dart';
class EconomicDegree extends StatelessWidget {
  const EconomicDegree({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
